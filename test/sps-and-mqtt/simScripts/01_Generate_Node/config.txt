mode=centralized

# Scales to generate (comma-separated)
# Options: TEST, SMALL, MEDIUM, LARGE, XLARGE
scales=TEST

# Zone patterns to generate (comma-separated)
# Options: UNIFORM, SINGLE_HOT_ZONE, DUAL_HOT_ZONES, TRIPLE_HOT_ZONES
patterns=UNIFORM

# Client type distribution (must sum to 100)
clients.customers=50
clients.trucks=30
clients.warehouses=20


#how would system perform with different loads under normal conditions/steady state conditions
# do multiple runs per setting. Get average performance for 5 - 10.
# 10000 messages per second e.g. total messages up to 100000.
# ramp up slowly
